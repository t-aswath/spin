'use client'
import { Login } from '@/components'

const Enter = () => {
    return (
        <main className="h-screen w-full flex">
            <Login />
        </main>
    )
}

export default Enter

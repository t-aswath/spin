'use client'
import { Signup } from '@/components'

const Enter = () => {
    return (
        <main className="h-screen w-full flex">
            <Signup />
        </main>
    )
}

export default Enter

